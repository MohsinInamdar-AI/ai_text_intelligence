version: "3.9"

services:
  api:
    build: .
    container_name: ai-text-intelligence-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
